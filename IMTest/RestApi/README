环境要求：
Python2.7
需要安装以下模块：
1、HTMLTestRunner
2、requests

布署说明：
1、注册org的管理员帐号
2、登陆管理员帐号创建app
3、获取appkey
4、将appkey、管理员帐号和密码更新到exec_test.sh文件中对应org,app,user,password的值，再配置rest服务器rest_server的值
5、在命令行直接运行shell脚本exec_test.sh执行测试，输出的结果为html文件默认在容器中的/data/tools目录，
    在docker容器中可挂载到宿主机目录下查看，命令行输出结果暂时用点"."表示测试正常，"F"表示测试失败，"E"表示异常情况（某些测试用例
    会依赖上一条测试的结果，因此上条报错后，下面的测试可能会抛异常），后期更新会将命令行输出直接打印测试结果。


备注：
可以使用python包管理工具来安装扩展，如：easy_install、pip等
